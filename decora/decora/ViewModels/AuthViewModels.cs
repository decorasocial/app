using System;
using System.Collections.Generic;
using System.Text;
using decora.Models;
using decora.Schemas;

namespace decora.ViewModels
{
    public class AuthViewModels
    {
       /* public Auth auth { get; set; }

        public AuthViewModels()
        {
            auth = new AuthDb().GetUser();
        }

        public void Update(Auth auth)
        {
            new AuthDb().update(auth);
        }

        public void Destroy()
        {
            new AuthDb().drop();
        }*/
    }
}
